name: viewfinder_example
description: Example app for the viewfinder package.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  viewfinder:
    path: ..

  taro: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
