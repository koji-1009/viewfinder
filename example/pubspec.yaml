name: viewfinder_example
description: Example app for the viewfinder package.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  viewfinder:
    path: ..

  go_router: ^17.5.0
  taro: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
